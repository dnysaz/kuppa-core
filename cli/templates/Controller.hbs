/**
 * {{name}} - Kuppa Framework
 * Standardized for The minimalist javascript supabase framework
 * Refactored with process.view() by Ketut Dana
 */

// Core & Application Dependencies
const BaseController = coreFile('controller.BaseController');
// const Model       = appFile('Models.yourModel'); 

class {{name}} extends BaseController {

    /**
     * [GET] Index Page
     */
    static async index(process) {
        try {
            // Context & Metadata
            const title       = 'List of {{name}}';
            const description = 'This is description for {{name}}';
            const view        = '{{name_lower}}.index';

            // Using the new Fluent View Interface
            return process.view(view).with({ 
                title,
                description
            });
        } catch (err) {
            process.next(err);
        }
    }

    /**
     * [GET] Create Page
     */
    static async create(process) {
        try {
            const title = 'Create New {{name}}';
            const view  = '{{name_lower}}.create';

            return process.view(view).with({ title });
        } catch (err) {
            process.next(err);
        }
    }

    // Add more logic for store, edit, update, destroy...
}

module.exports = {{name}};